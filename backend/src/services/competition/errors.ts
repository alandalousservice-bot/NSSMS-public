export type CompetitionErrorCode='NOT_FOUND'|'INVALID_STATE'|'INVALID_CONTEXT'|'CONFLICT'|'FORBIDDEN'|'VALIDATION_ERROR';
export class CompetitionError extends Error { constructor(public readonly code:CompetitionErrorCode,message:string,public readonly detail?:unknown){super(message)} }
export function mapCompetitionError(error:unknown):CompetitionError { const e=error as {code?:string;message?:string}; const message=e.message??'Competition operation failed'; if(e.code==='23505')return new CompetitionError('CONFLICT','The requested governed record already exists'); if(e.code==='23503')return new CompetitionError('INVALID_CONTEXT','A referenced governed record does not exist'); if(/not found|does not exist/i.test(message))return new CompetitionError('NOT_FOUND','The requested governed record was not found'); if(/immutable|frozen|forward-only|must begin|requires|stale|current|latest|matching|context|lifecycle|validation|archive|revocation|issuance/i.test(message))return new CompetitionError('INVALID_STATE','The requested operation is not valid for the current governed state'); return new CompetitionError('VALIDATION_ERROR','The competition request violates a database rule'); }
